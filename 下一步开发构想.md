为手机APP"聊天"更改UI 在聊天(群,联系人)界面中 发送的消息显示改为每一条都有气泡,自己发出的消息在右侧,其他人的在左侧,玩家名字改为头像+上方小字写ID 不用再显示时间

地图APP改为类似小地图mod(xaero's minimap)的地图渲染,只不过渲染区域默认为空,需使用/map set xyz <x1> <y1> <z1> <x2> <y2> <z2>,只渲染选定的区域内的地图,选定区域存储在世界文件夹下,在不改变比例拉伸 不遮挡元素的情况下尽量铺满APP显示区域
注册地图标点器 可以对着一个方块标点 手持时渲染所有标过的点(一个不断变换颜色的彩虹信标光柱) shift右键打开交互UI(风格类似手机) 可以查看所有标记的点,点进单个标记过的点可以查看位置,删除标点,或改变颜色(只影响在地图app查看时的颜色)
标点后,所有标点都可以被任何人在地图app看到,其本身不显示名字,在鼠标移动到标点上时显示名字

地图app是否显示当前位置的开关在管理员选项APP内,影响全局
地图APP是否显示标点的开关同上,影响全局
地图APP是否显示其他玩家位置同上,影响全局